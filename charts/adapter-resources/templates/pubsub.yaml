{{- range .Values.adapters }}
---
apiVersion: pubsub.cnrm.cloud.google.com/v1beta1
kind: PubSubSubscription
metadata:
  name: {{ .pubsub.subscriptionName }}
  annotations:
    cnrm.cloud.google.com/project-id: {{ $.Values.gcp.project | quote }}
spec:
  topicRef:
    name: {{ $.Values.pubsub.topic }}
  ackDeadlineSeconds: {{ .pubsub.ackDeadlineSeconds | default 60 }}
  messageRetentionDuration: {{ .pubsub.messageRetentionDuration | default "604800s" | quote }}
{{- end }}
